import XCTest
@testable import SwiftyLiveApi

final class SwiftyLiveApiTests: XCTestCase {
    // tests might be cool, but everything seems to be working
    
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        //        XCTAssertEqual(CodeScanner.text, "Hello, World!")
    }
    
    static var allTests = [
        ("testExample", testExample),
    ]
}
